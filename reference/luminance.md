# Perceived luminance of a hex colour

Uses the standard WCAG relative luminance formula.

## Usage

``` r
luminance(hex)
```

## Arguments

- hex:

  A `#rrggbb` hex colour string.

## Value

A numeric in `[0, 1]` (0 = black, 1 = white).
