% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/flag.R
\name{flag_data}
\alias{flag_data}
\title{Flag a data frame of metric values against the ANA reference}
\usage{
flag_data(df, ref)
}
\arguments{
\item{df}{A \code{data.frame} or tibble with one row per unit of analysis (UOA)
and one column per metric (e.g. \code{MET001}). A \code{uoa} column is expected but
not strictly required by this function — extra columns are passed through.}

\item{ref}{The reference list returned by \code{\link[=get_default_reference]{get_default_reference()}}.}
}
\value{
A tibble with the original columns plus metric-, subfactor-, factor-,
system-, and \code{prelim_flag} columns appended.
}
\description{
Applies the five-stage flagging pipeline:
\enumerate{
\item Per-metric flag / status / within-10-percent columns
\item Subfactor-level status (threshold-group rollup)
\item Factor-level status
\item System-level status
\item Preliminary flag (\code{prelim_flag}) via ANA decision tree
}
}
\details{
Preference-3 metrics are excluded from all flagging stages.
}
