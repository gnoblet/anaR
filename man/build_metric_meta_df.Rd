% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metric_metadata.R
\name{build_metric_meta_df}
\alias{build_metric_meta_df}
\title{Build a tidy tibble of metric metadata from reference JSON}
\usage{
build_metric_meta_df(json)
}
\arguments{
\item{json}{Parsed reference JSON list.}
}
\value{
A tibble with one row per metric and columns:
\code{metric_id}, \code{system_id}, \code{factor_id}, \code{subfactor_id},
\code{subfactor_path}, \code{factor_path}, \code{preference},
\code{an_threshold}, \code{direction}, \code{factor_threshold}, \code{evidence_threshold}.
}
\description{
Flattens the nested reference JSON (systems → factors → sub_factors →
indicators → metrics) into one row per metric. This tibble is the central
input for the flagging pipeline — downstream stages join against it rather
than re-traversing the JSON.
}
