% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate.R
\name{validate_csv}
\alias{validate_csv}
\title{Validate a CSV data frame against the ANA metric map}
\usage{
validate_csv(df, metric_map, opts = list())
}
\arguments{
\item{df}{A \code{data.frame} (or tibble) with one row per unit of analysis.
All values are expected to be character strings or \code{NA} (as produced by
\code{\link[readr:read_delim]{readr::read_csv()}} with \code{col_types = cols(.default = "c")}), but numeric
columns are also accepted and coerced to character for checking.}

\item{metric_map}{A named list keyed by metric ID (e.g. \code{"MET001"}). Each
value must be a list with at least a \code{type} element (the type string used
for cell-level validation).}

\item{opts}{A list of options:
\itemize{
\item \code{require_non_empty} (logical, default \code{FALSE}): if \code{TRUE}, empty cells
are treated as errors rather than missing values.
}}
}
\value{
An \code{ana_validation_result} list with elements:
\code{ok}, \code{header_errors}, \code{cell_errors} (tibble), \code{warnings},
\code{duplicate_uoas}, \code{missingness} (tibble), \code{metadata_cols}, \code{meta}.
}
\description{
Checks header structure, UOA uniqueness, and per-cell type constraints.
Returns an \code{ana_validation_result} object with full error detail.
}
